from ch05 import logRegress
import numpy as np

# dataSet,classLabels = logRegress.loadDataSet()
# # weights = logRegress.gradAscent(dataSet,classLabels)
# weights = logRegress.stocGradAscent1(np.array(dataSet),classLabels)
# print(weights)
# logRegress.plotBestFit(weights)

logRegress.multiTest()